kmsInit:
  enabled: true

minio:
  enabled: true
  endpoint: "http://minio:9000"
  username: kms-access-key-id
  password: kms-secret-access-key

kmsCoreClient:
  enabled: true
  nameOverride: kms-core-client
  image:
    name: ghcr.io/zama-ai/kms/core-client
    tag:
  num_majority: 2 # 2 when 4 parties and 5 when 13 parties
  num_reconstruct: 3 # 3 when 4 parties and 9 when 13 parties
  decryption_mode: NoiseFloodSmall
  fhe_parameter: Default
  #=============================================================
  #==========RESOURCERS TO ADJUST BASED ON ENVIRONMENT==========
  # If you launch the deployment locally, adjust these resources
  # according to your system and available resources
  resources:
    requests:
      memory: 24Gi
      cpu: 12
    limits:
      memory: 24Gi
      cpu: 12
  #=============================================================

kmsCore:
  enabled: false
  thresholdMode:
    enabled: true
    initializationScript:
      enabled: true
    peersList:
      - id: 1
        host: kms-service-threshold-1-kms-test-threshold-core-1
        port: 50001
      - id: 2
        host: kms-service-threshold-2-kms-test-threshold-core-2
        port: 50001
      - id: 3
        host: kms-service-threshold-3-kms-test-threshold-core-3
        port: 50001
      - id: 4
        host: kms-service-threshold-4-kms-test-threshold-core-4
        port: 50001

kubeUtils:
  image:
    name: ghcr.io/zama-ai/security-hub/infra/kube-utils
    tag: v0.4.3
    pullPolicy: Always

kmsGenKeys:
  enabled: false
kmsGenCertAndKeys:
  enabled: false
