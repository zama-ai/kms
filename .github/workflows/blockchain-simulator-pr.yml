name: "[Blockchain-Simulator-PR] Pipeline Run"

on:
  pull_request:
    paths:
      - 'blockchain/**'
      - 'core/**'
      - 'conf-trace/**'
      - '.github/workflows/blockchain-simulator-pr.yml'
      - 'Cargo.toml'

concurrency:
  group: blockchain-simulator-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/common-build-big-instance.yml
    with:
      working-directory: './blockchain/simulator'
      package-name: 'simulator'
      args-tests: '-- --skip full_gen_tests'
    permissions:
      contents: 'write'
      id-token: 'write'
    secrets:
      BLOCKCHAIN_ACTIONS_TOKEN: ${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}
      SLAB_ACTION_TOKEN: ${{ secrets.SLAB_ACTION_TOKEN }}
      SLAB_BASE_URL: ${{ secrets.SLAB_BASE_URL }}
      JOB_SECRET: ${{ secrets.JOB_SECRET }}

  done:
    runs-on: ubuntu-latest
    name: Pipeline Done
    steps:
      - name: Success
        run: echo Pipeline Done
    needs:
      - build
