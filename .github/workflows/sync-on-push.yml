# Sync repos
name: sync-on-push

on:
  pull_request:

  push:
    branches:
      - 'main'
  workflow_dispatch:

permissions: {}

jobs:
  sync-repo:
    name: sync-on-push/sync-repo
    if: ${{ github.repository == 'zama-ai/kms' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          lfs: true
          fetch-depth: 0
          persist-credentials: 'false'
          token: ${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}

      - name: git-sync
        env:
          SOURCE_REPO: "zama-ai/kms"
          SOURCE_BRANCH: "main"
          DESTINATION_BRANCH: "main"
          USERNAME: ${{ secrets.BOT_USERNAME }}
          TOKEN: ${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}
          DEST_REPO: ${{ secrets.SYNC_DEST_REPO }}
        run: |
          echo ">>> Cloning source repo..."
          git lfs install
          git clone "https://${USERNAME}:${TOKEN}@github.com/${SOURCE_REPO}.git" ./kms --origin source && cd ./kms
          git remote add destination "https://${USERNAME}:${TOKEN}@github.com/${DEST_REPO}.git"

          echo ">>> Fetching all branches references down locally so subsequent commands can see them..."
          git fetch source '+refs/heads/*:refs/heads/*' --update-head-ok

          # Print out all branches
          git --no-pager branch -a -vv

          # echo ">>> Fetching all LFS items from source..."
          # git lfs fetch --all source "${SOURCE_BRANCH}"

          # echo ">>> Pushing git changes..."
          # git push destination "${SOURCE_BRANCH}:${DESTINATION_BRANCH}" -f

          # echo ">>> Pushing all LFS items..."
          # git lfs push --all destination "${DESTINATION_BRANCH}"

      - name: debug
        if: always()
        run: git lfs logs last

      - name: git-sync-tags
        env:
          SOURCE_REPO: "zama-ai/kms"
          SOURCE_BRANCH: "refs/tags/*"
          DESTINATION_BRANCH: "refs/tags/*"
          USERNAME: ${{ secrets.BOT_USERNAME }}
          TOKEN: ${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}
          DEST_REPO: ${{ secrets.SYNC_DEST_REPO }}
        run: |
          echo ">>> Cloning source repo..."
          git clone "https://github.com/${SOURCE_REPO}.git" ./kms --origin source && cd ./kms
          git remote add destination "https://${USERNAME}:${TOKEN}@github.com/${DEST_REPO}.git"

          echo ">>> Fetching all branches references down locally so subsequent commands can see them..."
          git fetch source '+refs/heads/*:refs/heads/*' --update-head-ok

          # Print out all branches
          git --no-pager branch -a -vv

          # echo ">>> Pushing git changes..."
          # git push destination "${SOURCE_BRANCH}:${DESTINATION_BRANCH}" -f

          # echo ">>> Pushing all LFS items..."
          # git lfs push --all destination "${DESTINATION_BRANCH}"