name: Docker Build and Push nitro enclave

on:
  workflow_call:
    secrets:
      BLOCKCHAIN_ACTIONS_TOKEN:
        required: true
    inputs:
      ref:
        type: string
        required: false
        default: ''
      working-directory:
        type: string
        required: true
      docker-context:
        type: string
        required: false
        default: '.'
      image-name:
        type: string
        required: true
      image-enclave-name:
        type: string
        required: true
      push_image:
        type: boolean
        default: true
        required: false
      docker-file-enclave:
        type: string
        required: true
      cache-from:
        type: string
        required: false
        default: 'type=gha'
      cache-to:
        type: string
        required: false
        default: 'type=gha,mode=max'
    outputs:
      image_tag:
        description: "Docker Tag generated by this task"
        value: "${{ jobs.determine-tag.outputs.docker_tag }}"
      enclave_pcr0:
        description: "Enclave PCR0 generated by this task"
        value: "${{ jobs.build-and-push-enclave.outputs.enclave_pcr0 }}"
      enclave_pcr1:
        description: "Enclave PCR1 generated by this task"
        value: "${{ jobs.build-and-push-enclave.outputs.enclave_pcr1 }}"
      enclave_pcr2:
        description: "Enclave PCR2 generated by this task"
        value: "${{ jobs.build-and-push-enclave.outputs.enclave_pcr2 }}"


jobs:
  determine-tag:
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.set-tag.outputs.tag }}
    steps:
      - name: Checkout Project
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set Docker Tag
        id: set-tag
        run: |
          if [[ "${{ github.event_name }}" == "release" ]]; then
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          elif [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "tag=nightly-$(git rev-parse --short "$GITHUB_SHA")" >> "$GITHUB_OUTPUT"
          else
            echo "tag=$(git rev-parse --short "$GITHUB_SHA")" >> "$GITHUB_OUTPUT"
          fi

  build-and-push-enclave:
    needs: determine-tag
    outputs:
      enclave_pcr0: ${{ steps.eif.outputs.eif_pcr0 }}
      enclave_pcr1: ${{ steps.eif.outputs.eif_pcr1 }}
      enclave_pcr2: ${{ steps.eif.outputs.eif_pcr2 }}
    runs-on: 'ubuntu-latest'
    steps:
      - name: Checkout Project
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Get Rust version
        run: |
          version="$(cat ${{ github.workspace }}/toolchain.txt)"
          echo "RUST_IMAGE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb # v3.3.0
        with:
          platforms: linux/amd64

      - name: Login to GitHub Container Registry
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Echo github event
        run: echo "Github event ==> ${{ github.event_name }}"

      - name: Current branch sha
        if: github.event_name != 'release'
        run: |
          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "DOCKER_TAG_IMAGE=nightly-$(git rev-parse --short "$GITHUB_SHA")" >> "$GITHUB_ENV"
          else
            echo "DOCKER_TAG_IMAGE=$(git rev-parse --short "$GITHUB_SHA")" >> "$GITHUB_ENV"
          fi

      - name: Current Tag
        if: github.event_name == 'release'
        run: |
          echo "DOCKER_TAG_IMAGE=${{  github.ref_name }}" >> "$GITHUB_ENV"

      # Our self-hosted runner AMI doesn't ship jq which we need during EIF build
      - name: Install jq
        run: |
          sudo apt update && sudo apt -y install jq

      - name: Set EIF output dir
        run: |
          echo "EIF_OUTPUT_DIR=${{ github.workspace }}/${{ inputs.working-directory }}/eif-output" >> "$GITHUB_ENV"

      - name: Create EIF file
        uses: addnab/docker-run-action@4f65fabd2431ebc8d299f8e5a018d79a769ae185 #v3
        with:
          image: amazonlinux@sha256:5fe11d17e56571a26e20df3fa1493bdf9139fb9f2cb6d84fad13104c7b869217 # 2023.5.20240916.0
          options: -v ${{ env.EIF_OUTPUT_DIR }}:/output -v ${{env.DOCKER_SOCK_PATH}}:/var/run/docker.sock
          run: |
            dnf install aws-nitro-enclaves-cli aws-nitro-enclaves-cli-devel docker -y

            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u USERNAME --password-stdin

            docker pull --platform=linux/amd64 ghcr.io/zama-ai/${{ inputs.image-name }}:${{ needs.determine-tag.outputs.docker_tag }}

            nitro-cli build-enclave --docker-uri ghcr.io/zama-ai/${{ inputs.image-name }}:${{ needs.determine-tag.outputs.docker_tag }} --name ${{ inputs.image-name }} --output-file /output/enclave.eif

            nitro-cli describe-eif --eif-path /output/enclave.eif > /output/eif-info.txt

      - name: Export EIF measurements
        id: eif
        run: |
          EIF_MEASUREMENT_PCR0=$(jq '.Measurements.PCR0' ${{ env.EIF_OUTPUT_DIR }}/eif-info.txt)
          EIF_MEASUREMENT_PCR1=$(jq '.Measurements.PCR1' ${{ env.EIF_OUTPUT_DIR }}/eif-info.txt)
          EIF_MEASUREMENT_PCR2=$(jq '.Measurements.PCR2' ${{ env.EIF_OUTPUT_DIR }}/eif-info.txt)
          {
            echo "eif_pcr0=${EIF_MEASUREMENT_PCR0}"
            echo "eif_pcr1=${EIF_MEASUREMENT_PCR1}"
            echo "eif_pcr2=${EIF_MEASUREMENT_PCR2}"
          } >> "$GITHUB_OUTPUT"

      - name: Docker Build and Push Enclave Image
        uses: docker/build-push-action@5cd11c3a4ced054e52742c5fd54dca954e0edd85 # v6.7.0
        with:
          context: .
          secrets: BLOCKCHAIN_ACTIONS_TOKEN=${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}
          build-args: |
            IMAGE_NAME=ghcr.io/zama-ai/${{ inputs.image-name }}
            IMAGE_TAG=${{ needs.determine-tag.outputs.docker_tag }}
            RUST_IMAGE_VERSION=${{ env.RUST_IMAGE_VERSION }}
          build-contexts: eif=${{ env.EIF_OUTPUT_DIR }}
          file: ./docker/${{ inputs.working-directory}}/${{ inputs.docker-file-enclave }}
          push: ${{ inputs.push_image }}
          pull: false
          tags: |
            ghcr.io/zama-ai/${{ inputs.image-enclave-name }}:${{ needs.determine-tag.outputs.docker_tag }}
            ghcr.io/zama-ai/${{ inputs.image-enclave-name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          labels: |
            zama.kms.version=${{ needs.determine-tag.outputs.docker_tag }}
            zama.kms.eif_pcr0=${{ steps.eif.outputs.eif_pcr0 }}

      - name: Export image name
        id: export-image
        run: echo "image=ghcr.io/zama-ai/${{inputs.image-name}}:${{ needs.determine-tag.outputs.docker_tag }}" >> "${GITHUB_OUTPUT}"

      ###########################################################
      # Release Part for EIF
      # EIF is only released on release events and added to the assets
      ###########################################################
      - name: Add eif-info.txt to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631  # v2.2.2
        with:
          files: |
            ${{ env.EIF_OUTPUT_DIR }}/eif-info.txt