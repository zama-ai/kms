# Main workflow for KMS Core that handles testing and build
# Triggers:
# 1. Scheduled: Every weekday at 00:00 UTC (01:00 CET) for nightly tests and build
# 2. Pull requests: For validation before merging
# 3. Pushes: On main and release/* branches for building images
# IMPORTANT NOTES: The tests are only executed for components that have been changed
name: main

on:
  schedule:
    - cron: '0 0 * * 1-5' # Runs at midnight UTC (1 AM CET) Monday-Friday
  pull_request:
  push:
    branches: ['main', 'release/*']

# Controls concurrent workflow runs:
# - Groups runs by git ref
# - Cancels in-progress runs for non-main/release branches
concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

# Top-level permissions for workflow-level operations
permissions:
  actions: read # Required to read workflow run information
  contents: read # Required to checkout repository code
  pull-requests: read # Required to read pull request information
  packages: read # Required to read GitHub packages/container registry

jobs:
  # Initial job that determines which components have changed
  # Used by subsequent jobs to decide whether they need to run
  check-changes:
    name: main/check-changes
    # job permissions
    permissions:
      actions: read # Required to read workflow run information
      contents: read # Required to checkout repository code
      id-token: write # Required for OIDC token generation
      pull-requests: read # Required to read pull request information
    runs-on: ubuntu-latest
    outputs:
      # Each output indicates if files in a specific component were modified.
      # Only outputs consumed by jobs in *this* workflow are exported here.
      # NOTE: the per-package test matrix in `parallel-testing.yml` does NOT
      # consume these outputs - it currently runs unconditionally (no change
      # filter) so every PR exercises and warms the shared rust cache. Wire a
      # filter back in once the caching approach is validated.
      changes-ci: ${{ steps.filter.outputs.ci }}
      changes-core-client-nightly-tests: ${{ steps.filter.outputs.core-client-nightly-tests }}
      changes-core-service: ${{ steps.filter.outputs.core-service }}
      changes-workspace-crates: ${{ steps.filter.outputs.workspace-crates }}
      changes-docs: ${{ steps.filter.outputs.docs }}
    steps:
      # TODO: remove once GitHub runner images ship the CVE-2026-31431 kernel fix
      - name: Workaround CVE-2026-31431 (copy.fail)
        run: |
          echo "install algif_aead /bin/false" | sudo tee /etc/modprobe.d/disable-algif-aead.conf
          if lsmod | grep -q algif_aead; then
            sudo rmmod algif_aead || echo "WARNING: rmmod failed - module may be in use"
          elif modinfo algif_aead 2>/dev/null | grep -q builtin; then
            echo "WARNING: algif_aead built-in - modprobe.d blacklist has no effect"
          fi

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: true
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        id: filter
        with:
          # Define paths that trigger specific component workflows
          # Changes to observability affect multiple components
          filters: |
            ci:
              - '.github/workflows/**'
              - 'rust-toolchain.toml'
            core-client-nightly-tests:
              - 'core-client/tests/kind-testing/**'
              - 'core-client/tests/integration/**'
            core-service:
              - 'backward-compatibility/**'
              - 'core/grpc/**'
              - 'core/service/**'
              - 'core/threshold-*/**'
              - 'core/test-utils/**'
              - 'core/thread-handles/**'
              - 'observability/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'rust-toolchain.toml'
            # Workspace crates that have no dedicated CI job of their own.
            # Used by `test-workspace-crates` to run their unit tests grouped
            # across a few runners.
            workspace-crates:
              - 'bc2wrap/**'
              - 'core/error-utils/**'
              - 'core/test-utils/**'
              - 'core/thread-handles/**'
              - 'core/threshold-*/**'
              - 'observability/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'rust-toolchain.toml'
            docs:
              - 'docs/**'

  ############################################################################
  # Parallel testing pipeline
  # Runs UNCONDITIONALLY on every PR/push (no change filter, no `needs`): this
  # is intentional while we validate the shared rust cache, so every run
  # exercises and warms it. Tighten with a change filter once that's settled.
  ############################################################################
  parallel-testing:
    name: main/parallel-testing
    uses: ./.github/workflows/parallel-testing.yml
    permissions:
      checks: write # Required to write to the GitHub Actions check suite
      packages: read # Required to read from the GitHub Packages registry
      issues: write # Required to write to the GitHub Issues API
      pull-requests: write # Required to write to the GitHub Pull Requests API
      actions: read # Required to read the GitHub Actions API
      contents: read # Required to read the GitHub Contents API
    secrets: inherit

  ############################################################################
  # Docker build pipeline
  # Triggered by:
  # - Label : "Docker" on pull request
  ############################################################################
  docker-build:
    name: docker-build
    if: github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'docker')
    permissions:
      actions: read # Required to read workflow run information
      contents: write # Required to checkout repository code
      id-token: write # Required for OIDC authentication
      pull-requests: read # Required to read pull requests information
      packages: write # Required to publish Docker images
      attestations: write # Required to create build attestations
    uses: ./.github/workflows/docker-build.yml
    secrets: inherit

  ############################################################################
  # KMS Local Docs Link Check
  # Triggered by changes to docs/** (and every push to main).
  ############################################################################
  check-docs:
    name: main/check-docs
    # job permissions
    permissions:
      actions: read # Required to read workflow run information
      contents: read # Required to checkout repository code
      id-token: write # Required for OIDC token generation
      pull-requests: read # Required to read pull request information
    needs: check-changes
    if: needs.check-changes.outputs.changes-docs == 'true' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    container:
      image: python:3.14-slim-bookworm@sha256:a9bee15510a364124aa24692899d269835683b883de42f7ebec8c293cf679ccb
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - name: Install safe-chain
# Safe-chain proxies npm and python package installs and checks for malware. Defense against supply-chain attacks.
        run: curl -fsSL https://github.com/AikidoSec/safe-chain/releases/download/1.5.3/install-safe-chain.sh | sh -s -- --ci
      - run: python3 -m pip install linkcheckmd
      - name: Check dead-link
        run: python3 ci/scripts/local_docs_link_check.py

  ############################################################################
  # Core Client nightly pipeline
  # Note: regular per-partition core-client/core-service/core-grpc/core-threshold
  # test jobs live in `.github/workflows/parallel-testing.yml` (see the `test-matrix`
  # job in that file). This workflow only keeps the schedule-driven nightly
  # job for `kms-core-client`.
  ############################################################################
  # Core client nightly tests (runs ALL tests including nightly and full_gen_tests).
  # Triggered by scheduled runs, or by PRs touching core-client/tests/{kind-testing,integration}/**.
  test-core-client-nightly:
    name: main/test-core-client-nightly
    # job permissions
    permissions:
      checks: write # Required to create GitHub checks for test results
      packages: read # Required to read GitHub packages/container registry
      issues: write # Required to create comments on issues
      pull-requests: write # Required to create comments on pull requests
      actions: read # Required to read workflow run information and download artifacts
      contents: read # Required to checkout repository code
    needs: check-changes
    if: >-
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' && needs.check-changes.outputs.changes-core-client-nightly-tests == 'true')
    uses: ./.github/workflows/common-testing.yml
    with:
      crate-names: '-p kms-core-client'
      # Run ALL tests except k8s_* (cluster-only), isolated_test_example (demo),
      # and the docker-tls test which has its own dedicated job.
      # nightly_* and nightly_full_gen_tests_* are intentionally included here
      # (they're skipped only in the regular-CI profile via --skip).
      args-tests: >-
        --features threshold_tests -- --skip k8s_ --skip isolated_test_example
        --skip test_threshold_mpc_context_switch_6_docker
      nextest-profile: 'ci-nightly'
      runs-on: '64cpu-linux-x64'
      nextest-test-threads: "10"
    secrets: inherit

  ############################################################################
  # Workspace crates
  # Runs clippy + unit tests for workspace crates that have no dedicated CI
  # job. Crates are split into four groups to amortize runner setup cost.
  # Triggered by:
  # - Changes to any of the grouped crates (filter: workspace-crates)
  # - Changes to CI workflows
  ############################################################################
  test-workspace-crates:
    name: main/test-workspace-crates
    permissions:
      checks: write # Required to create GitHub checks for test results
      packages: read # Required to read GitHub packages/container registry
      issues: write # Required to create comments on issues
      pull-requests: write # Required to create comments on pull requests
      actions: read # Required to read workflow run information and download artifacts
      contents: read # Required to checkout repository code
    needs: check-changes
    secrets: inherit
    if: >-
      needs.check-changes.outputs.changes-workspace-crates == 'true' ||
      needs.check-changes.outputs.changes-ci == 'true' ||
      github.ref == 'refs/heads/main'
    strategy:
      fail-fast: false
      # Groups aim at similarly-sized work. `runtime-slow` is split out because enabling `slow_tests` takes a long time.
      matrix:
        include:

          # "Normal" crates and small utilities; default features only.
          # threshold-execution / threshold-bgv / threshold-networking are
          # intentionally excluded — heavy-1 / heavy-2 already run them with
          # `*/slow_tests`, which is a *superset* of the default test set.
          - group: crates-normies
            crates: '-p threshold-types -p threshold-hashing -p threshold-algebra -p bc2wrap -p error-utils -p thread-handles -p test-utils -p observability'
            features: ''
            partition: ''
            threads: ''
            runs-on: '64cpu-linux-x64'

          # Slow/heavy tests, 1/2. Big iron, lots of RAM.
          # threshold-execution slow_tests, 3-way partitioned (~16min each).
          - group: crates-heavy-1-p1
            crates: '-p threshold-execution'
            features: '-F threshold-execution/slow_tests'
            partition: '--partition count:1/3'
            threads: '8'
            runs-on: '64cpu-linux-x64'
          - group: crates-heavy-1-p2
            crates: '-p threshold-execution'
            features: '-F threshold-execution/slow_tests'
            partition: '--partition count:2/3'
            threads: '8'
            runs-on: '64cpu-linux-x64'
          - group: crates-heavy-1-p3
            crates: '-p threshold-execution'
            features: '-F threshold-execution/slow_tests'
            partition: '--partition count:3/3'
            threads: '8'
            runs-on: '64cpu-linux-x64'

          # Slow/heavy tests, 2/2. Big iron, lots of RAM.
          # The `threshold-bgv` tests all use N65536 and is inherently RAM-heavy, so gets own runner.
          # TODO(dp): discuss making this non-required so failures here do not impeed PR merge?
          - group: crates-heavy-2-fast
            crates: '-p threshold-bgv'
            features: '-F threshold-bgv/slow_tests'
            partition: '-- --skip test_dkg_with_offline --skip test_dkg_dummy_preproc'
            threads: '8'
            runs-on: '64cpu-linux-x64'
          # threshold-bgv DKG tests (~30min, NOT required for PR merge —
          # update branch protection to drop this from required checks).
          - group: crates-heavy-2-dkg
            crates: '-p threshold-bgv'
            features: '-F threshold-bgv/slow_tests'
            # No spaces/quotes: args-tests is forwarded via unquoted word-splitting,
            # so the whole filterset must be a single shell token (`+` = union).
            partition: '-E test(/test_dkg_with_offline/)+test(/test_dkg_dummy_preproc/)'
            threads: '2'
            runs-on: '64cpu-linux-x64'
    uses: ./.github/workflows/common-testing.yml
    with:
      # --no-tests=pass: no tests is not an error, but to nexttest it is and this is the fix.
      # matrix.partition carries per-group nextest args (e.g. `--partition count:1/3`,
      # `-E '...'`, or `-- --skip ...`); it must be forwarded or every job runs the full suite.
      args-tests: '--no-tests=pass ${{ matrix.features }} ${{ matrix.partition }}'
      crate-names: ${{ matrix.crates }}
      nextest-test-threads: ${{ matrix.threads }}
      skip-test-material: true
      runs-on: ${{ matrix.runs-on }}

  # Test reporting job that runs after all tests complete.
  # Only runs on pull requests to generate test reports.
  # Note: per-package test jobs that moved to `parallel-testing.yml` are reported by
  # that workflow's own `test-reporter` job. This reporter only covers the
  # jobs that still live in this workflow.
  test-reporter:
    name: main/test-reporter
    if: ${{ always() && github.event_name == 'pull_request' }}
    needs:
      - test-core-client-nightly
      - test-workspace-crates
      - test-wasm
    uses: ./.github/workflows/test-reporter.yml
    with:
      initial-workflow: 'main'
    permissions:
      checks: write # Required to create GitHub checks for test results
      packages: read # Required to read GitHub packages/container registry
      issues: write # Required to create comments on issues
      pull-requests: write # Required to create comments on pull requests
      actions: read # Required to read workflow run information and download artifacts
      contents: read # Required to checkout repository code
    secrets: inherit

  ############################################################################
  # Wasm Testing Pipeline
  # Triggered by changes to core/service/** or .github/workflows/**.
  ############################################################################
  test-wasm:
    name: main/test-wasm
    # job permissions
    permissions:
      checks: write # Required to create GitHub checks for test results
      packages: read # Required to read GitHub packages/container registry
      pull-requests: write # Required to create comments on pull requests
      actions: read # Required to read workflow run information and download artifacts
      contents: read # Required to checkout repository code
    needs:
      - check-changes
    if: >-
      needs.check-changes.outputs.changes-core-service == 'true' ||
      needs.check-changes.outputs.changes-ci == 'true'
    uses: ./.github/workflows/wasm-testing.yml
    secrets: inherit
