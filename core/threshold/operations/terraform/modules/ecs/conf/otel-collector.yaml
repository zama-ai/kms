receivers:
  otlp:
    protocols:
      grpc:
exporters:
  otlp:
    endpoint: observability.ddec.dev:4317
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
